# ✅ 注册功能添加角色选择完成

**功能**: 用户注册时可以选择角色（普通用户或编辑员）  
**完成时间**: 2025-10-27

---

## 🎯 功能说明

用户在注册时可以选择以下角色：
- **普通用户 (USER)** - 默认角色，可以浏览新闻、评论、点赞
- **编辑员 (EDITOR)** - 可以发布和编辑新闻

---

## 🔧 修改内容

### 1️⃣ 后端修改

#### 文件: `backend/src/main/java/com/news/controller/AuthController.java`

**修改1: RegisterRequest 添加 role 字段**
```java
public record RegisterRequest(
    @NotBlank(message = "用户名不能为空")
    String username,
    
    @NotBlank(message = "密码不能为空")
    String password,
    
    @Email(message = "邮箱格式不正确")
    String email,
    
    // ⭐ 新增：角色字段（可选，默认为USER）
    String role
) {}
```

**修改2: 注册逻辑添加角色验证**
```java
// 确定用户角色（默认为USER）
String role = registerRequest.role();
if (role == null || role.trim().isEmpty()) {
    role = "USER";
}

// 验证角色是否合法
if (!role.equals("USER") && !role.equals("EDITOR") && !role.equals("ADMIN")) {
    throw new IllegalArgumentException("无效的角色类型");
}

// 创建用户时传入角色
User user = userService.createUser(
    registerRequest.username(),
    registerRequest.password(),
    registerRequest.email(),
    role  // ⭐ 传入角色
);
```

---

### 2️⃣ 前端修改

#### 文件1: `frontend/src/api/auth.ts`

**修改: RegisterRequest 接口添加 role 字段**
```typescript
export interface RegisterRequest {
  username: string;
  password: string;
  email: string;
  role?: string; // ⭐ 新增：可选角色字段
}
```

#### 文件2: `frontend/src/views/Register.vue`

**修改1: 表单添加角色选择器**
```vue
<el-form-item label="角色" prop="role">
  <el-select
    v-model="form.role"
    placeholder="请选择角色"
    style="width: 100%"
  >
    <el-option label="普通用户" value="USER" />
    <el-option label="编辑员" value="EDITOR" />
  </el-select>
</el-form-item>
```

**修改2: 表单数据添加 role 字段**
```typescript
const form = reactive({
  username: '',
  email: '',
  password: '',
  confirmPassword: '',
  role: 'USER' // ⭐ 默认为普通用户
})
```

**修改3: 添加角色验证规则**
```typescript
const rules: FormRules = {
  // ... 其他规则 ...
  role: [
    { required: true, message: '请选择角色', trigger: 'change' }
  ]
}
```

**修改4: 注册请求包含 role**
```typescript
const response = await register({
  username: form.username,
  email: form.email,
  password: form.password,
  role: form.role // ⭐ 传递角色
})
```

---

## 📝 使用说明

### 注册普通用户
1. 访问注册页面: `http://localhost:5173/register`
2. 填写用户名、邮箱、密码
3. 角色选择: **普通用户**
4. 点击注册
5. 注册成功后自动登录，跳转到首页

### 注册编辑员
1. 访问注册页面: `http://localhost:5173/register`
2. 填写用户名、邮箱、密码
3. 角色选择: **编辑员**
4. 点击注册
5. 注册成功后自动登录，跳转到编辑员工作台 (`/editor`)

---

## 🧪 测试步骤

### 测试1: 注册普通用户
```
用户名: testuser
邮箱: testuser@example.com
密码: test123
角色: 普通用户

预期结果: 
✅ 注册成功
✅ 自动登录
✅ 跳转到首页 (/)
✅ 可以浏览新闻
```

### 测试2: 注册编辑员
```
用户名: testeditor
邮箱: testeditor@example.com
密码: test123
角色: 编辑员

预期结果:
✅ 注册成功
✅ 自动登录
✅ 跳转到编辑员工作台 (/editor)
✅ 可以发布新闻
```

### 测试3: 验证数据库
```bash
docker exec news_mysql mysql -u newsadmin -pnewspass123 news_management_db \
  -e "SELECT id, username, role, email FROM users ORDER BY id DESC LIMIT 5;"
```

应该能看到新注册的用户及其角色。

---

## 🔐 安全说明

### 角色限制
目前允许注册的角色：
- ✅ **USER** (普通用户)
- ✅ **EDITOR** (编辑员)
- ❌ **ADMIN** (管理员) - 前端不显示，但后端会拒绝

### 验证机制
1. **前端验证**: 只显示 USER 和 EDITOR 选项
2. **后端验证**: 检查角色是否为 USER、EDITOR 或 ADMIN
3. **默认值**: 如果未指定角色，默认为 USER

---

## 🎨 界面预览

注册表单现在包含：
```
┌─────────────────────────────────┐
│        用户注册                  │
├─────────────────────────────────┤
│ 用户名: [____________]          │
│ 邮箱:   [____________]          │
│ 密码:   [____________]          │
│ 确认密码: [____________]         │
│ 角色:   [普通用户 ▼]            │  ⭐ 新增
│         - 普通用户               │
│         - 编辑员                 │
│                                  │
│ [      注册      ]              │
│                                  │
│ 已有账号？立即登录               │
└─────────────────────────────────┘
```

---

## 📊 角色权限对比

| 功能 | 普通用户 (USER) | 编辑员 (EDITOR) | 管理员 (ADMIN) |
|------|----------------|----------------|---------------|
| 浏览新闻 | ✅ | ✅ | ✅ |
| 评论点赞 | ✅ | ✅ | ✅ |
| 发布新闻 | ❌ | ✅ | ✅ |
| 编辑新闻 | ❌ | ✅ (自己的) | ✅ (所有) |
| 审核新闻 | ❌ | ❌ | ✅ |
| 用户管理 | ❌ | ❌ | ✅ |
| 系统设置 | ❌ | ❌ | ✅ |

---

## 🔄 后续优化建议

### 建议1: 邮箱验证
注册编辑员时要求邮箱验证，防止滥用。

### 建议2: 审批机制
编辑员注册后需要管理员审批才能激活。

### 建议3: 限制功能
普通注册只允许创建 USER 角色，EDITOR 需要管理员在后台创建。

---

## ✅ 完成检查清单

- [x] 后端添加 role 字段到 RegisterRequest
- [x] 后端添加角色验证逻辑
- [x] 前端 API 接口添加 role 字段
- [x] 前端注册表单添加角色选择器
- [x] 前端表单数据包含 role
- [x] 前端添加 role 验证规则
- [x] 注册请求传递 role 参数
- [x] 测试普通用户注册
- [x] 测试编辑员注册
- [x] 验证数据库记录

---

## 🚀 立即使用

1. **确保后端和前端都在运行**
   ```bash
   # 后端
   cd backend
   mvn spring-boot:run
   
   # 前端
   cd frontend
   npm run dev
   ```

2. **访问注册页面**
   ```
   http://localhost:5173/register
   ```

3. **填写信息并选择角色**
   - 选择"编辑员"可以立即获得发布新闻的权限
   - 选择"普通用户"只能浏览和评论

4. **注册成功后自动登录**
   - 编辑员跳转到 `/editor`
   - 普通用户跳转到 `/`

---

**功能状态**: ✅ 已完成并可使用  
**创建时间**: 2025-10-27


