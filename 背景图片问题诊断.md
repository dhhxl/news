# 背景图片问题诊断指南

## 当前状态
- ✅ 图片文件已放置在 `frontend/public/images/istockphoto-1419410282-2048x2048.jpg`
- ✅ CSS 样式已更新为使用该图片作为背景
- ✅ 前端服务器正在端口 5173 运行

## 诊断步骤

### 1. 测试图片是否可以直接访问
访问：`http://localhost:5173/test-image.html`

这个测试页面会显示：
- 直接的 `<img>` 标签图片
- CSS背景图片测试
- 直接访问图片的链接

### 2. 检查网络请求
1. 打开浏览器开发者工具 (F12)
2. 切换到 "Network" / "网络" 选项卡
3. 刷新登录页面
4. 查找 `istockphoto-1419410282-2048x2048.jpg` 的请求
5. 检查状态码：
   - 200: 图片加载成功
   - 404: 图片文件未找到
   - 其他: 网络或服务器问题

### 3. 强制清除缓存
- **Chrome/Edge**: Ctrl + Shift + R
- **Firefox**: Ctrl + F5
- 或者在开发者工具中右击刷新按钮，选择"清空缓存并硬刷新"

### 4. 检查Console错误
在开发者工具的Console选项卡中查看是否有相关错误信息。

## 修复方案

### 如果图片仍然不显示：

1. **重启服务器**：
   ```bash
   # 停止前端服务 (Ctrl+C)
   cd frontend
   npm run dev
   ```

2. **检查文件权限**：确保图片文件有读取权限

3. **尝试重命名图片**：
   - 将图片重命名为简单的名称，如 `forest.jpg`
   - 更新CSS中的路径

4. **使用base64编码** (备用方案)：
   如果路径问题无法解决，可以将图片转换为base64编码直接嵌入CSS。

## 当前CSS设置
```css
.login-container {
  background-color: #2e7d32; /* 备用绿色背景 */
  background-image: url('/images/istockphoto-1419410282-2048x2048.jpg');
  background-size: cover;
  background-position: center center;
  background-repeat: no-repeat;
}
```

## 联系方式
如果问题仍然存在，请提供：
1. 浏览器类型和版本
2. 开发者工具Network选项卡的截图
3. Console中的错误信息
