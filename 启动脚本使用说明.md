# 🚀 启动脚本使用说明

## 📋 脚本列表

本项目提供了4个批处理脚本，方便快速启动和管理系统：

| 脚本名称 | 功能说明 | 使用场景 |
|---------|---------|---------|
| `启动前端.bat` | 仅启动前端服务 | 后端已启动，只需启动前端 |
| `启动后端.bat` | 仅启动后端服务 | 前端已启动，只需启动后端 |
| `一键启动全部.bat` | 自动启动所有服务 | **推荐**：首次启动或完整启动 |
| `停止所有服务.bat` | 停止所有运行的服务 | 需要停止系统时使用 |

---

## 🎯 快速开始（推荐）

### 方式一：一键启动（最简单）

**双击运行 `一键启动全部.bat`**

脚本会自动按顺序启动：
1. 数据库服务（MySQL + Redis）
2. 后端服务（Spring Boot）
3. 前端服务（Vite）

启动完成后：
- 前端地址：http://localhost:5173
- 后端地址：http://localhost:8080/api

### 方式二：分别启动

如果需要单独启动某个服务：

1. **启动后端**：双击 `启动后端.bat`
2. **启动前端**：双击 `启动前端.bat`

---

## 📝 详细说明

### 1️⃣ 启动前端.bat

**功能**：启动前端开发服务器

**执行步骤**：
1. 检查 Node.js 是否安装
2. 检查并安装前端依赖（首次运行）
3. 启动 Vite 开发服务器

**端口**：http://localhost:5173

**日志**：
- 会显示编译进度
- 显示可访问的地址
- 显示热更新信息

**停止方式**：在命令窗口按 `Ctrl+C`

---

### 2️⃣ 启动后端.bat

**功能**：启动后端Spring Boot服务

**执行步骤**：
1. 检查 Java 和 Maven 是否安装
2. 检查数据库服务是否运行
3. 自动启动数据库（如果未运行）
4. 启动 Spring Boot 应用

**端口**：http://localhost:8080/api

**日志**：
- 显示Spring Boot启动日志
- 显示数据库连接状态
- 显示API端点映射

**停止方式**：在命令窗口按 `Ctrl+C`

---

### 3️⃣ 一键启动全部.bat

**功能**：自动启动所有必需的服务

**执行步骤**：
1. 检查系统环境（Java、Maven、Node.js、Docker）
2. 启动数据库服务（docker-compose up -d）
3. 在新窗口启动后端服务
4. 在新窗口启动前端服务
5. 等待所有服务就绪

**特点**：
- ✅ 自动检查环境
- ✅ 按正确顺序启动
- ✅ 等待服务启动完成
- ✅ 在独立窗口运行（互不影响）

**启动后会打开3个窗口**：
1. 主窗口：显示启动状态
2. 后端窗口：显示后端日志
3. 前端窗口：显示前端日志

**停止方式**：
- 方式1：运行 `停止所有服务.bat`
- 方式2：关闭所有打开的命令窗口

---

### 4️⃣ 停止所有服务.bat

**功能**：一键停止所有运行中的服务

**执行步骤**：
1. 停止后端服务进程
2. 停止前端服务进程
3. 停止数据库容器（docker-compose down）

**使用场景**：
- 需要完全停止系统
- 切换到其他项目
- 释放端口和资源

---

## ⚙️ 系统要求

### 必需软件

| 软件 | 版本要求 | 用途 | 下载地址 |
|------|---------|------|----------|
| Java JDK | 17+ | 运行后端 | https://adoptium.net/ |
| Maven | 3.6+ | 构建后端 | https://maven.apache.org/ |
| Node.js | 16+ | 运行前端 | https://nodejs.org/ |
| Docker Desktop | 最新版 | 运行数据库 | https://www.docker.com/products/docker-desktop |

### 端口占用

确保以下端口未被占用：
- **5173**：前端开发服务器
- **8080**：后端API服务
- **3306**：MySQL数据库
- **6379**：Redis缓存

---

## 🐛 常见问题

### Q1: 提示"未检测到Java"

**解决方案**：
1. 下载并安装 JDK 17 或更高版本
2. 配置环境变量 `JAVA_HOME`
3. 将 `%JAVA_HOME%\bin` 添加到 `PATH`
4. 重启命令窗口

### Q2: 提示"未检测到Maven"

**解决方案**：
1. 下载并解压 Maven
2. 配置环境变量 `MAVEN_HOME`
3. 将 `%MAVEN_HOME%\bin` 添加到 `PATH`
4. 重启命令窗口

### Q3: 提示"未检测到Node.js"

**解决方案**：
1. 下载并安装 Node.js LTS版本
2. 安装时勾选"Add to PATH"
3. 重启命令窗口

### Q4: 数据库启动失败

**解决方案**：
1. 确保 Docker Desktop 正在运行
2. 检查 3306 和 6379 端口是否被占用
3. 手动运行：`docker-compose up -d`
4. 查看日志：`docker-compose logs`

### Q5: 端口被占用

**解决方案**：
```bash
# 查看端口占用
netstat -ano | findstr "5173"
netstat -ano | findstr "8080"
netstat -ano | findstr "3306"

# 结束占用端口的进程
taskkill /F /PID <进程ID>
```

### Q6: 前端依赖安装失败

**解决方案**：
```bash
cd frontend
npm cache clean --force
npm install --registry=https://registry.npmmirror.com
```

### Q7: 后端编译失败

**解决方案**：
```bash
cd backend
mvn clean install -DskipTests
```

---

## 🎓 使用技巧

### 技巧1：查看详细日志

如果启动失败，查看窗口中的详细错误信息。

### 技巧2：开发时只启动需要的服务

- 只修改前端：运行 `启动前端.bat`
- 只修改后端：运行 `启动后端.bat`

### 技巧3：检查服务状态

**检查后端**：
```bash
curl http://localhost:8080/api/health
```

**检查前端**：
浏览器访问 http://localhost:5173

**检查数据库**：
```bash
docker ps | findstr news_mysql
```

### 技巧4：快速重启

1. 按 `Ctrl+C` 停止服务
2. 在同一窗口按 ↑（上箭头）
3. 按 `Enter` 重新运行

---

## 📊 启动流程图

```
一键启动全部.bat
    ↓
[检查环境]
├─ Java ✓
├─ Maven ✓
├─ Node.js ✓
└─ Docker ✓
    ↓
[启动数据库]
├─ MySQL (3306)
└─ Redis (6379)
    ↓ (等待15秒)
[启动后端]
└─ Spring Boot (8080)
    ↓ (等待20秒)
[启动前端]
└─ Vite Dev Server (5173)
    ↓
✓ 全部启动完成
```

---

## 🔧 自定义配置

如需修改默认端口或其他配置：

### 修改前端端口

编辑 `frontend/vite.config.ts`：
```typescript
server: {
  port: 5173, // 修改这里
  host: '0.0.0.0'
}
```

### 修改后端端口

编辑 `backend/src/main/resources/application.yml`：
```yaml
server:
  port: 8080  # 修改这里
```

### 修改数据库端口

编辑 `docker-compose.yml`：
```yaml
ports:
  - "3306:3306"  # 修改这里
```

---

## 📞 获取帮助

如果遇到问题：
1. 查看命令窗口的错误信息
2. 检查本文档的"常见问题"部分
3. 查看项目中的其他文档（README.md等）

---

## ✅ 验证启动成功

启动后，确认以下内容：

### 1. 数据库
```bash
docker ps
# 应该看到 news_mysql 和 news_redis 容器在运行
```

### 2. 后端
浏览器访问：http://localhost:8080/api/news
应该返回新闻列表JSON数据

### 3. 前端
浏览器访问：http://localhost:5173
应该看到新闻系统首页

### 4. 登录测试
- 管理员：admin / admin123
- 编辑员：editor / editor123

---

**创建时间**：2025-10-28  
**适用版本**：新闻管理系统 v1.0  
**脚本类型**：Windows批处理文件（.bat）

