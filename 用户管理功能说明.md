# 👥 用户管理功能说明

## 📋 功能概述

管理员可以通过用户管理模块查看、管理系统中的所有用户，包括查看用户详情、禁用/启用用户、删除用户、重置密码等操作。

---

## ✨ 主要功能

### 1. 用户列表查看
- ✅ 分页显示所有用户
- ✅ 显示用户基本信息（ID、头像、用户名、姓名、邮箱、手机号）
- ✅ 显示用户角色（管理员/编辑员/普通用户）
- ✅ 显示用户状态（正常/已禁用）
- ✅ 显示注册时间和最后登录时间

### 2. 搜索和筛选
- ✅ 关键词搜索（支持用户名、邮箱、姓名）
- ✅ 按角色筛选
- ✅ 按状态筛选（正常/已禁用）
- ✅ 重置筛选条件

### 3. 用户详情查看
- ✅ 查看用户完整信息
- ✅ 查看用户头像
- ✅ 查看用户活跃情况

### 4. 用户管理操作
- ✅ 禁用用户（停止用户登录）
- ✅ 启用用户（恢复用户登录）
- ✅ 删除用户（永久删除，不可恢复）
- ✅ 重置密码（管理员强制重置用户密码）

### 5. 用户统计
- ✅ 总用户数
- ✅ 管理员数量
- ✅ 编辑员数量
- ✅ 普通用户数量

---

## 🔐 权限说明

### 访问权限
- **仅管理员**可以访问用户管理功能
- 编辑员和普通用户无法访问

### 操作限制
- ❌ **不能删除**管理员账户
- ❌ **不能禁用**管理员账户
- ✅ 可以删除编辑员和普通用户
- ✅ 可以禁用/启用编辑员和普通用户
- ✅ 可以重置任何非管理员用户的密码

---

## 🚀 使用方法

### 访问用户管理页面

1. 使用管理员账号登录
   ```
   用户名: admin
   密码: admin123
   ```

2. 在管理后台侧边栏，点击「用户管理」菜单

3. 进入用户管理页面

### 查看用户列表

进入用户管理页面后，会自动加载用户列表，显示：
- 用户头像
- 用户名
- 姓名
- 邮箱
- 手机号
- 角色标签
- 状态标签
- 注册时间
- 最后登录时间
- 操作按钮

### 搜索用户

1. **关键词搜索**
   - 在搜索框输入用户名、邮箱或姓名
   - 点击「搜索」按钮或按回车

2. **角色筛选**
   - 选择角色：全部/管理员/编辑员/普通用户
   - 自动刷新列表

3. **状态筛选**
   - 选择状态：全部/正常/已禁用
   - 自动刷新列表

4. **重置筛选**
   - 点击「重置」按钮清除所有筛选条件

### 查看用户详情

1. 在用户列表中找到目标用户
2. 点击「详情」按钮
3. 弹出用户详情对话框，显示：
   - 用户头像（大图）
   - 所有个人信息
   - 注册时间
   - 最后登录时间
   - 账户状态

### 禁用/启用用户

#### 禁用用户
1. 在用户列表中找到要禁用的用户
2. 点击「禁用」按钮（黄色）
3. 确认操作
4. **效果**：用户无法登录系统

**注意**：
- 不能禁用管理员账户
- 被禁用的用户会话立即失效

#### 启用用户
1. 在用户列表中找到已禁用的用户
2. 点击「启用」按钮（绿色）
3. 确认操作
4. **效果**：用户恢复登录权限

### 删除用户

1. 在用户列表中找到要删除的用户
2. 点击「删除」按钮（红色）
3. 确认删除操作
4. **警告**：此操作不可恢复！

**注意**：
- 不能删除管理员账户
- 删除用户会删除其所有相关数据
- 建议优先使用「禁用」功能

### 重置用户密码

1. 点击用户的「详情」按钮
2. 在详情对话框中，点击「重置密码」按钮
3. 输入新密码（至少6位）
4. 点击「确认重置」
5. **效果**：用户密码立即更改为新密码

**注意**：
- 重置后应立即通知用户新密码
- 建议用户登录后立即修改密码

---

## 📊 用户统计面板

页面顶部显示用户统计信息：

```
┌─────────────────────────────────────────┐
│  总用户: 156人                            │
│  管理员: 2人                              │
│  编辑员: 15人                             │
│  普通用户: 139人                          │
└─────────────────────────────────────────┘
```

统计信息实时更新，反映当前系统用户分布。

---

## 🎨 界面说明

### 用户角色标签
- 🔴 **管理员** - 红色标签
- 🟠 **编辑员** - 橙色标签
- ⚪ **普通用户** - 灰色标签

### 用户状态标签
- 🟢 **正常** - 绿色标签
- 🔴 **已禁用** - 红色标签

### 操作按钮
- 🔵 **详情** - 蓝色按钮
- 🟡 **禁用** - 黄色按钮（仅正常用户显示）
- 🟢 **启用** - 绿色按钮（仅已禁用用户显示）
- 🔴 **删除** - 红色按钮（不显示在管理员行）

---

## 🔧 技术实现

### 后端API

#### 1. 获取用户列表
```
GET /api/admin/users
参数:
  - page: 页码（从0开始）
  - size: 每页大小
  - keyword: 搜索关键词（可选）
  - role: 角色筛选（可选）
  - isEnabled: 状态筛选（可选）
  
返回:
  - users: 用户列表
  - totalElements: 总记录数
  - totalPages: 总页数
  - currentPage: 当前页
  - pageSize: 每页大小
```

#### 2. 获取用户详情
```
GET /api/admin/users/{userId}

返回: 用户完整信息
```

#### 3. 删除用户
```
DELETE /api/admin/users/{userId}

返回: 成功/失败
```

#### 4. 禁用/启用用户
```
PUT /api/admin/users/{userId}/toggle-status

返回: 更新后的用户信息
```

#### 5. 重置密码
```
POST /api/admin/users/{userId}/reset-password
请求体:
{
  "newPassword": "新密码"
}

返回: 成功/失败
```

#### 6. 用户统计
```
GET /api/admin/users/statistics

返回:
{
  "total": 总用户数,
  "admins": 管理员数,
  "editors": 编辑员数,
  "users": 普通用户数
}
```

### 前端组件
- **页面**: `frontend/src/views/admin/UserManagement.vue`
- **API**: `frontend/src/api/userManagement.ts`
- **路由**: `/admin/users`

---

## ⚠️ 安全注意事项

### 1. 删除用户前的检查
- 确认是否需要保留用户数据
- 建议先禁用用户观察一段时间
- 删除操作不可恢复

### 2. 重置密码
- 重置后立即通知用户
- 建议使用强密码
- 要求用户首次登录后修改密码

### 3. 权限保护
- 所有API都有管理员权限验证
- 前端路由也有权限守卫
- 非管理员无法访问

### 4. 操作日志
- 所有管理操作都会记录到审计日志
- 可以在「审计日志」页面查看操作历史

---

## 🐛 常见问题

### Q1: 为什么看不到用户管理菜单？
**A**: 请确认：
- 已使用管理员账号登录
- 账号角色是 `ADMIN`
- 前端服务已重启（如果刚添加功能）

### Q2: 删除用户时提示"不能删除管理员账户"？
**A**: 这是系统保护机制，管理员账户不能被删除，只能手动修改数据库。

### Q3: 禁用用户后，用户是否还能访问系统？
**A**: 不能。被禁用的用户：
- 无法登录系统
- 已有会话会失效
- 需要管理员启用后才能再次登录

### Q4: 重置密码后，用户的原密码还能用吗？
**A**: 不能。重置密码会立即生效，旧密码失效。

### Q5: 删除用户会删除用户创建的内容吗？
**A**: 取决于系统配置：
- 用户创建的新闻会保留
- 用户的评论可能会保留
- 建议查看具体业务逻辑

---

## 📝 操作记录

所有用户管理操作都会记录到审计日志：
- 删除用户
- 禁用/启用用户
- 重置密码

可以在「审计日志」页面查看详细记录。

---

## 🔄 更新记录

### V1.0.0 - 2025-10-28
- ✅ 新增用户管理模块
- ✅ 支持用户列表查看（分页）
- ✅ 支持搜索和筛选
- ✅ 支持查看用户详情
- ✅ 支持禁用/启用用户
- ✅ 支持删除用户
- ✅ 支持重置密码
- ✅ 新增用户统计面板
- ✅ 完善权限控制
- ✅ 添加操作确认对话框

---

## 📞 技术支持

如有问题，请联系系统管理员。

---

**祝您使用愉快！** 🎉

