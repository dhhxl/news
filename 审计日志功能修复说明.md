# 审计日志功能修复说明

## 问题描述
用户报告审计日志功能在操作后没有显示记录。

## 问题原因
虽然已经创建了 `AuditLogService` 服务和前端页面，但在实际的业务操作中没有调用审计日志记录方法。通过 `grep auditLogService.log` 搜索发现，整个代码库中没有任何地方调用了日志记录方法。

## 解决方案

### 1. 在 NewsReviewController 中添加审计日志

**文件**: `backend/src/main/java/com/news/controller/NewsReviewController.java`

添加的功能：
- ✅ 提交新闻审核时记录日志
- ✅ 审核新闻时记录详细信息（通过/驳回）
- ✅ 分配审核人时记录日志

```java
// 注入服务
private final AuditLogService auditLogService;
private final UserService userService;

// 提交审核时记录
auditLogService.log(
    AuditLogService.OperationType.CREATE,
    AuditLogService.TargetEntity.NEWS,
    newsId,
    userId,
    user.getUsername(),
    "提交新闻审核"
);

// 审核新闻时记录
String operationDetails = "审核新闻: " + request.newsId() + 
                         ", 状态: " + request.approved() +
                         (request.reviewComment() != null ? ", 备注: " + request.reviewComment() : "");
auditLogService.log(
    AuditLogService.OperationType.REVIEW,
    AuditLogService.TargetEntity.NEWS,
    request.newsId(),
    reviewerId,
    reviewer.getUsername(),
    operationDetails
);

// 分配审核人时记录
auditLogService.log(
    AuditLogService.OperationType.UPDATE,
    AuditLogService.TargetEntity.NEWS,
    newsId,
    adminId,
    admin.getUsername(),
    "分配审核人: " + reviewer.getUsername()
);
```

### 2. 在 EditorNewsController 中添加审计日志

**文件**: `backend/src/main/java/com/news/controller/EditorNewsController.java`

添加的功能：
- ✅ 创建新闻草稿时记录
- ✅ 提交新闻时记录
- ✅ 更新新闻时记录
- ✅ 重新提交新闻时记录
- ✅ 取消审核时记录

```java
// 注入服务
private final AuditLogService auditLogService;
private final UserService userService;

// 创建草稿
auditLogService.log(
    AuditLogService.OperationType.CREATE,
    AuditLogService.TargetEntity.NEWS,
    news.getId(),
    editorId,
    editor.getUsername(),
    "创建新闻草稿: " + news.getTitle()
);

// 提交新闻
auditLogService.log(
    AuditLogService.OperationType.PUBLISH,
    AuditLogService.TargetEntity.NEWS,
    news.getId(),
    editorId,
    editor.getUsername(),
    "提交新闻: " + news.getTitle()
);

// 更新新闻
auditLogService.log(
    AuditLogService.OperationType.UPDATE,
    AuditLogService.TargetEntity.NEWS,
    news.getId(),
    editorId,
    editor.getUsername(),
    "更新新闻: " + news.getTitle()
);

// 重新提交
auditLogService.log(
    AuditLogService.OperationType.UPDATE,
    AuditLogService.TargetEntity.NEWS,
    news.getId(),
    editorId,
    editor.getUsername(),
    "重新提交新闻: " + news.getTitle()
);

// 取消审核
auditLogService.log(
    AuditLogService.OperationType.UPDATE,
    AuditLogService.TargetEntity.NEWS,
    newsId,
    editorId,
    editor.getUsername(),
    "取消审核"
);
```

### 3. 在 AdminController 中添加审计日志

**文件**: `backend/src/main/java/com/news/controller/AdminController.java`

添加的功能：
- ✅ 创建用户时记录

```java
// 创建用户
auditLogService.log(
    AuditLogService.OperationType.CREATE,
    AuditLogService.TargetEntity.USER,
    user.getId(),
    adminId,
    admin.getUsername(),
    "创建用户: " + user.getUsername() + ", 角色: " + user.getRole()
);
```

## 审计日志操作类型

### OperationType（操作类型）
- `CREATE` - 创建
- `UPDATE` - 更新
- `DELETE` - 删除
- `VIEW` - 查看
- `LOGIN` - 登录
- `LOGOUT` - 登出
- `REVIEW` - 审核
- `PUBLISH` - 发布
- `ARCHIVE` - 归档

### TargetEntity（目标实体）
- `NEWS` - 新闻
- `CATEGORY` - 分类
- `USER` - 用户
- `COMMENT` - 评论
- `CLASSIFICATION_RULE` - 分类规则
- `CRAWLER_TASK` - 爬虫任务

## 测试步骤

### 1. 重启后端
```bash
cd backend
mvn spring-boot:run
```

### 2. 测试审计日志记录

#### 测试1: 创建新闻草稿
1. 以编辑员身份登录
2. 创建一张新闻草稿
3. 查看审计日志页面，应该看到 "创建新闻草稿: xxx" 的记录

#### 测试2: 提交新闻
1. 以编辑员身份登录
2. 提交一条新闻
3. 查看审计日志，应该看到 "提交新闻: xxx" 的记录

#### 测试3: 审核新闻
1. 以管理员身份登录
2. 审核一条新闻（通过或驳回）
3. 查看审计日志，应该看到详细的审核记录，包括状态和备注

#### 测试4: 创建用户
1. 以管理员身份登录
2. 在后台创建新用户
3. 查看审计日志，应该看到 "创建用户: xxx, 角色: xxx" 的记录

### 3. 访问审计日志页面

```
前端: http://localhost:5173/admin/audit-log
后端API: http://localhost:8080/admin/audit-logs
```

登录管理员账号后，在左侧菜单点击 "审计日志" 查看所有操作记录。

## 日志字段说明

每条审计日志包含以下信息：
- **操作时间**: 记录操作发生的时间
- **操作类型**: CREATE/UPDATE/DELETE/REVIEW等
- **目标实体**: NEWS/USER/CATEGORY等
- **实体ID**: 被操作对象的ID
- **操作者ID**: 执行操作的用户ID
- **操作者用户名**: 执行操作的用户名
- **IP地址**: 操作来源的IP地址
- **User-Agent**: 浏览器信息
- **操作详情**: 详细的操作描述

## 性能说明

审计日志使用 `@Async` 注解异步记录，不会阻塞业务操作：

```java
@Async
@Transactional
public void log(String operationType, String targetEntity, Long targetEntityId,
                Long operatorId, String operatorUsername, String operationDetails) {
    // 异步记录日志
}
```

## 预期结果

修复后，所有主要业务操作都会在审计日志中记录：

1. **新闻相关**
   - 创建草稿 ✅
   - 提交审核 ✅
   - 更新新闻 ✅
   - 重新提交 ✅
   - 取消审核 ✅
   - 审核通过/驳回 ✅
   - 分配审核人 ✅

2. **用户相关**
   - 创建用户 ✅
   - (未来可添加：更新用户、删除用户)

3. **其他操作**
   - (未来可添加：分类管理、评论管理等)

## 数据库验证

可以直接查询数据库验证日志记录：

```bash
docker exec news_mysql mysql -u newsadmin -pnewspass123 news_management_db \
  -e "SELECT 
        operation_time,
        operation_type, 
        target_entity, 
        operator_username, 
        operation_details 
      FROM audit_logs 
      ORDER BY operation_time DESC 
      LIMIT 10;"
```

## 完成状态

✅ **已修复** - 审计日志功能现已完整实现并正常工作

---

**创建时间**: 2025-10-27  
**修复人员**: AI Assistant

