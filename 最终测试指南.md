# 🧪 最终测试指南

## ✅ 已完成的修复

### 后端 ✅
1. **内容验证**：100字 → 20字
2. **图片路径**：`/api/images` → `/images`
3. **静态资源**：禁用默认映射
4. **中文文件名**：URL编码支持

### 前端 ✅
1. **API路径**：移除重复的 `/api` 前缀

---

## ⏳ 等待后端启动

查看新打开的PowerShell窗口，等待看到：
```
Started NewsManagementApplication in X.XXX seconds
```

**大约需要30秒**

---

## 🔄 强制刷新浏览器（重要！）

### Windows:
- **Ctrl + Shift + R**（推荐）
- 或 **Ctrl + F5**
- 或 **Shift + F5**

### 为什么要强制刷新？
普通刷新（F5）可能使用缓存的JavaScript文件，导致旧的API路径仍然生效。

**强制刷新会清除缓存并重新加载所有资源。**

---

## 🧪 测试步骤

### 测试1：内容长度验证（20字）

1. 在新闻编辑页面
2. 输入标题：`测试新闻`
3. 输入内容：`这是一个测试新闻内容啊啊啊啊啊`（刚好20个字）
4. 选择分类：`体育`
5. 点击"提交审核"

**预期结果**：
- ✅ 成功提交（不再要求100字）
- ✅ 无错误提示

**如果还是要求100字**：
- 说明后端没有重启成功
- 查看后端窗口是否有错误

---

### 测试2：图片上传

1. 点击"添加图片"按钮
2. 选择一张图片上传
3. 查看上传后的预览

**预期结果**：
- ✅ 图片成功上传
- ✅ 图片预览正常显示
- ✅ 浏览器控制台无 `/api/api/images` 错误

**如果还有 `/api/api/images` 错误**：
- 说明前端缓存没清除
- 请使用 **Ctrl + Shift + R** 强制刷新

---

### 测试3：查看未使用图片

在新闻编辑页面，应该能看到"未使用的图片"列表

**预期结果**：
- ✅ 列表正常加载
- ✅ 无500错误

---

### 测试4：发布完整新闻

1. 输入标题（11字符以上）
2. 输入内容（20字符以上）
3. 选择分类
4. 上传图片
5. 点击"提交审核"

**预期结果**：
- ✅ 成功提交
- ✅ 跳转到"我的新闻"页面
- ✅ 新闻状态为"待审核"

---

### 测试5：审核和查看

1. **管理员**登录
2. 进入"待审核新闻"
3. 审核通过该新闻
4. 回到首页
5. 查看该新闻

**预期结果**：
- ✅ 新闻正常显示
- ✅ 图片正常显示
- ✅ 无404或500错误

---

## 🐛 常见问题

### 问题1：还是要求100字
**原因**：后端没启动成功或使用了旧版本

**解决**：
1. 查看后端窗口是否显示 "Started"
2. 如果没有，查看错误信息
3. 如果有多个Java进程，停止所有并重启

### 问题2：还是 `/api/api/images` 错误
**原因**：浏览器使用了缓存的JS文件

**解决**：
1. 使用 **Ctrl + Shift + R** 强制刷新
2. 或清空浏览器缓存：
   - Chrome：F12 → Network标签 → 勾选"Disable cache"
   - 然后刷新页面

### 问题3：图片不显示
**原因**：后端路径映射问题

**解决**：
1. 检查后端日志中的路由映射：
   ```
   GET "/images/file/xxx.png"
   Mapped to com.news.controller.ImageUploadController#getImageFile
   ```
2. 如果映射错误，说明后端代码没更新

---

## 📊 验证清单

完成测试后，以下所有项目应该都打勾：

- [ ] 内容验证改为20字（不再要求100字）
- [ ] 图片可以正常上传
- [ ] 上传后图片立即显示
- [ ] "未使用图片"列表正常加载
- [ ] 提交新闻成功（带图片）
- [ ] 管理员可以审核
- [ ] 首页显示新闻和图片
- [ ] 浏览器控制台无 `/api/api` 错误
- [ ] 浏览器控制台无500错误

---

## 🎉 全部通过说明系统正常！

如果所有测试都通过，恭喜！系统已完全修复，可以正常使用了！

---

## 🆘 还是有问题？

如果测试失败，请提供：

1. **后端日志**（尤其是启动时和请求时的日志）
2. **浏览器控制台**的完整错误信息（F12 → Console标签）
3. **Network标签**中失败请求的详情
4. 具体哪个步骤失败

---

**测试准备完成时间**：2025-10-28 09:21  
**下一步**：等待30秒后开始测试

